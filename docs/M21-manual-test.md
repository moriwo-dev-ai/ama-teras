# M21 手動確認(実行中の対話性と可視化)

自動テスト(vitest・typecheck 3構成)は2026-07-06夜間に全合格。以下は体感確認用。

## M21-1 実行中の追加指示

- [ ] 長めのタスクを実行中に、入力欄が有効のまま(プレースホルダが「追加の指示を入力…」に変わる)
- [ ] 実行中に送信すると「↩ 追加指示(次のターンで反映)」バッジ付きの吹き出しが出て、
      エージェントが次のターンでその指示を拾う(エラーにならない)
- [ ] 複数回連続で追加指示を送っても全部反映される
- [ ] モデルが応答を書き終えるタイミングで追加指示を送っても、取りこぼさずループが継続する
- [ ] 「停止」は従来どおり即キャンセル(未反映の追加指示は破棄される)
- [ ] スマホ(remote-ui)からも実行中に「↩追加」で送れる。どちらの画面にも吹き出しが出る

## M21-2 サブエージェント同時数

- [ ] Settings →「サブエージェント同時実行数」セレクタ(1〜8・既定3)がある
- [ ] 5に設定して dispatch_agent parallel に5タスク渡すと5体同時に走る
- [ ] 6タスク渡すとツールがエラーを返す(「最大 5 件まで」)

## M21-3 QR表示

- [ ] Settings → リモートアクセス: ホスト名入力済みならQRが表示される(config保存なので再起動後も残る)
- [ ] アプリ再起動後(平文トークンなし)は「トークン無しQR+📱トークン込みQRを出す(再生成)」ボタン
- [ ] 再生成ボタン → 確認ダイアログ → トークン込みQRが表示される

## M21-4 生存と考えの可視化

- [ ] 実行中、受領指示の直下に「現在の状況」行(スピナー+経過時間+最新思考の末尾)がライブ更新
- [ ] 入力欄の上にもスピナー+状態+経過時間が常時表示される
- [ ] 実行中のツールカードに経過時間、bash実行中はstdout末尾が流れる
- [ ] 出力が30秒止まると「⏳無応答Xs」が出る(実行は止めない)
- [ ] エージェントタブ: work子に💭最新思考+経過時間、スピナーが回る
- [ ] アニメーションOFF(Settings)や OSの「視差効果を減らす」でスピナーが静止する
