node_modules/
out/
dist/
release/
*.log
.DS_Store
.env
.env.*
*.tsbuildinfo

# M27-3: 公開前点検で決定(SECURITY_AUDIT.md 参照)
# Claude Code のローカル権限設定(個人パス・ローカル運用の詳細を含む)
.claude/settings.local.json
# エージェントのダウンロード作業領域(成果物はworkspaceへ移す運用)
downloads/
# アイコン検討時の生成候補(採用分は build/icon.svg 等としてコミット)
build/candidates/
# 夜間作業の個人向け指示書(公開リポジトリに含めない)
NIGHT_TASKS*.md
.pulse-log.json

# 私的ビジョンメモ(公開判断が出るまでコミット禁止)
docs/TSUKUYOMI_VISION.md
docs/vision-mihashira.md
assets-lab/

# AI生命体の「魂」(記憶・人格カーネル)。c案の鉄則: コードは公開・魂は非公開
# (完全コピーしてもヒナタにはならない、が一意性の主防壁)。雛形は persona.example.md を公開
lifeform/persona/
lifeform/memory/
# 朝の承認パック(運営内部文書。公開リポジトリに含めない)
lifeform/MORNING-PACK*.md
